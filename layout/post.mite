<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<title><? STR(page->title) STR(" - ") STR(global.title) ?></title>
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<meta name="description" content="<? STR(global.description) ?>">
		<link rel="stylesheet" href="/style.css">
	</head>
	<body>
		<div class="container">
			<header>
				<nav class="site-nav">
					<a href="/">Ana Sayfa</a>
					<a href="/about">Hakkımda</a>
				</nav>
				<div class="dashed"></div>

				<h1><? STR(page->title) ?></h1>

				<? if (page->date) { ?>
				<p class="date"><? STR(page->date) ?></p>
				<? } ?>

				<? if (page->tags) { ?>
				<p class="tags"><? STR(page->tags) ?></p>
				<? } ?>

			</header>

			<main class="content">
				<? CONTENT() ?>
			</main>

			<? INCLUDE("footer") ?>
		</div>
	</body>
</html>


